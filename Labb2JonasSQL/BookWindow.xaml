<Window x:Class="Labb2JonasSQL.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Labb2JonasSQL"
        mc:Ignorable="d"
        Title="Book Window" Height="450" Width="800">
    <DataGrid x:Name="bookDataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN13}" />
            <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
            <DataGridTextColumn Header="Language" Binding="{Binding Language}" />
            <DataGridTextColumn Header="Price" Binding="{Binding Price}" />
            <DataGridTextColumn Header="Publication Date" Binding="{Binding PublicationDate}" />
        </DataGrid.Columns>
    </DataGrid>
</Window>


